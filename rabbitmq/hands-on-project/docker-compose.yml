services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: order_system_rabbitmq
    ports:
      - "5672:5672"    # AMQP
      - "15672:15672"  # management UI
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 5s
      timeout: 5s
      retries: 5
